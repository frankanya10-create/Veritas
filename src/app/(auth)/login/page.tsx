import LoginClient from "./LoginClient";

export const metadata = {
  title: "Sign In — Veritas",
  description: "Sign in to your Veritas compliance intelligence account.",
};

export default function LoginPage() {
  return <LoginClient />;
}
