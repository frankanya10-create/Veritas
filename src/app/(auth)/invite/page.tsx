import InviteClient from "./InviteClient";

export const metadata = {
  title: "Workspace Invitation — Veritas",
  description: "Accept your workspace invitation.",
};

export default function InvitePage() {
  return <InviteClient />;
}
